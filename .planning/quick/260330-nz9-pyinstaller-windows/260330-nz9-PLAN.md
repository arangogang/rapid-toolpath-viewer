---
phase: quick
plan: 260330-nz9
type: execute
wave: 1
depends_on: []
files_modified:
  - rapid_viewer.spec
  - build.bat
  - pyproject.toml
autonomous: true
requirements: []
must_haves:
  truths:
    - "dist/rapid_viewer.exe 파일이 생성된다"
    - "rapid_viewer.exe를 실행하면 MainWindow가 뜬다"
    - "rapid_viewer.exe에서 .mod 파일을 열 수 있다"
    - "build.bat를 실행하면 전체 빌드가 자동으로 완료된다"
  artifacts:
    - path: "rapid_viewer.spec"
      provides: "PyInstaller 빌드 명세 (hidden imports, Qt plugins 포함)"
    - path: "build.bat"
      provides: "재빌드용 Windows 배치 스크립트"
    - path: "dist/rapid_viewer.exe"
      provides: "단일 실행파일 (Python 불필요)"
  key_links:
    - from: "rapid_viewer.spec"
      to: "src/rapid_viewer/main.py"
      via: "entrypoints.script → main() 호출"
    - from: "rapid_viewer.spec"
      to: "PyQt6 Qt plugins"
      via: "collect_data / hidden-imports로 플랫폼 플러그인 번들링"
---

<objective>
PyInstaller로 rapid_viewer.exe Windows 단일 실행파일을 빌드한다.

Purpose: 로봇 엔지니어가 Python 없이 .exe만 실행해서 .mod 파일을 검증할 수 있어야 한다.
Output: dist/rapid_viewer.exe, rapid_viewer.spec, build.bat
</objective>

<execution_context>
@C:/Users/sovia/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

Entry point: src/rapid_viewer/main.py — main() 함수
Stack: PyQt6 + PyOpenGL (현재 pyproject.toml에는 PyOpenGL 미포함이나 향후 추가 예정)
패키지 소스: src/ 레이아웃 (hatchling 빌드백엔드)
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyproject.toml에 pyinstaller dev 의존성 추가</name>
  <files>pyproject.toml</files>
  <action>
pyproject.toml의 [project.optional-dependencies] dev 섹션에 pyinstaller>=6.0 추가:

```toml
[project.optional-dependencies]
dev = [
    "pytest>=9.0",
    "pytest-qt>=4.4",
    "pyinstaller>=6.0",
]
```

그 다음 uv로 설치:

```bash
uv add --dev pyinstaller>=6.0
```

pyinstaller가 설치되었는지 확인:
```bash
uv run pyinstaller --version
```
  </action>
  <verify>
    <automated>uv run pyinstaller --version</automated>
  </verify>
  <done>pyinstaller --version 출력에 버전 번호가 표시됨</done>
</task>

<task type="auto">
  <name>Task 2: rapid_viewer.spec 작성 및 빌드 실행</name>
  <files>rapid_viewer.spec, build.bat</files>
  <action>
프로젝트 루트에 rapid_viewer.spec 파일을 작성한다. src/ 레이아웃이므로 pathex에 src/ 포함, PyQt6 플러그인 자동 수집, 향후 PyOpenGL 추가 대비 hidden-imports 선언.

**rapid_viewer.spec 내용:**

```python
# -*- mode: python ; coding: utf-8 -*-
from PyInstaller.utils.hooks import collect_data_files, collect_submodules

# PyQt6 Qt 플랫폼 플러그인 수집 (windows, styles 등)
datas = collect_data_files('PyQt6', includes=['Qt6/plugins/**'])

# rapid_viewer 패키지 모든 서브모듈 수집 (parser, ui 등)
hidden_imports = (
    collect_submodules('rapid_viewer')
    + [
        'PyQt6.QtWidgets',
        'PyQt6.QtGui',
        'PyQt6.QtCore',
        'PyQt6.QtOpenGL',
        'PyQt6.QtOpenGLWidgets',
        # OpenGL — 향후 PyOpenGL 추가 시 활성화
        # 'OpenGL',
        # 'OpenGL.GL',
        # 'OpenGL.arrays.numpymodule',
        # 'OpenGL_accelerate',
        'numpy',
    ]
)

a = Analysis(
    ['src/rapid_viewer/main.py'],
    pathex=['src'],
    binaries=[],
    datas=datas,
    hiddenimports=hidden_imports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[
        'tkinter',
        'matplotlib',
        'scipy',
        'PIL',
        'IPython',
    ],
    noarchive=False,
)

pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='rapid_viewer',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,          # UPX 압축 — 바이러스 오탐 방지를 위해 비활성화
    console=False,      # 콘솔 창 없이 GUI 앱으로 실행
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=None,          # 아이콘 없음 (향후 추가 가능: icon='assets/icon.ico')
)
```

**build.bat 내용** (프로젝트 루트에 생성):

```bat
@echo off
setlocal

echo === ABB RAPID Toolpath Viewer - Windows Build ===
echo.

REM 기존 빌드 산출물 정리
echo [1/3] Cleaning previous build...
if exist build rmdir /s /q build
if exist dist  rmdir /s /q dist

REM PyInstaller 빌드
echo [2/3] Running PyInstaller...
uv run pyinstaller rapid_viewer.spec
if errorlevel 1 (
    echo ERROR: PyInstaller build failed.
    exit /b 1
)

REM 결과 확인
echo [3/3] Verifying output...
if exist dist\rapid_viewer.exe (
    echo.
    echo Build successful!
    echo Output: dist\rapid_viewer.exe
    for %%F in (dist\rapid_viewer.exe) do echo Size:   %%~zF bytes
) else (
    echo ERROR: dist\rapid_viewer.exe not found.
    exit /b 1
)

endlocal
```

파일 작성 후 빌드 실행:

```bash
uv run pyinstaller rapid_viewer.spec
```

빌드 중 누락된 hidden-import 경고가 있으면 spec의 hiddenimports 목록에 추가 후 재빌드.

**주요 트러블슈팅:**
- `ModuleNotFoundError: No module named 'rapid_viewer'` → pathex=['src'] 확인
- Qt 플랫폼 플러그인 오류 (`qt.qpa.plugin: Could not find the Qt platform plugin`) → collect_data_files('PyQt6', ...) 확인
- `--onefile` 대신 `--onefile` 모드 사용 시 startup이 느려짐 → 현재 스펙은 단일 EXE (onefile) 모드
  </action>
  <verify>
    <automated>test -f dist/rapid_viewer.exe && echo "EXE exists" || echo "MISSING"</automated>
  </verify>
  <done>
dist/rapid_viewer.exe가 존재한다.
파일 크기가 10MB 이상 (Qt 플러그인 포함 기준).
dist\rapid_viewer.exe를 더블클릭하면 MainWindow가 뜨고 File > Open으로 .mod 파일을 열 수 있다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
dist/rapid_viewer.exe — PyInstaller 단일 실행파일
build.bat — 재빌드 스크립트
  </what-built>
  <how-to-verify>
1. dist\rapid_viewer.exe 더블클릭 — MainWindow 창이 열려야 함
2. File > Open (Ctrl+O) 클릭 — .mod 파일 다이얼로그가 열려야 함
3. 테스트용 .mod 파일 선택 — 오류 없이 타이틀바에 파일명이 표시되어야 함
4. 창을 닫고 build.bat 더블클릭 — 빌드 성공 메시지가 출력되어야 함
  </how-to-verify>
  <resume-signal>"approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<verification>
- dist/rapid_viewer.exe 파일 존재 확인: `test -f dist/rapid_viewer.exe`
- EXE 크기 확인 (Qt 포함 시 통상 30-80MB): `ls -lh dist/rapid_viewer.exe`
- build.bat 실행 성공 여부 (exit code 0)
- EXE 직접 실행하여 MainWindow 표시 확인 (human-verify 체크포인트)
</verification>

<success_criteria>
- dist/rapid_viewer.exe가 Python 없이 실행된다
- MainWindow가 정상 표시되고 File > Open이 동작한다
- build.bat 실행으로 clean 빌드가 완료된다
- rapid_viewer.spec이 향후 PyOpenGL 추가 시 주석 해제만으로 대응 가능하다
</success_criteria>

<output>
완료 후 .planning/quick/260330-nz9-pyinstaller-windows/260330-nz9-SUMMARY.md 생성
</output>
