---
phase: 01-parser-and-file-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/rapid_viewer/__init__.py
  - src/rapid_viewer/parser/__init__.py
  - src/rapid_viewer/parser/tokens.py
  - src/rapid_viewer/parser/patterns.py
  - src/rapid_viewer/ui/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/fixtures/simple.mod
  - tests/fixtures/multiline.mod
  - tests/fixtures/movecircular.mod
  - tests/fixtures/moveabsj.mod
  - tests/fixtures/offs_inline.mod
  - tests/test_parser.py
autonomous: true
requirements:
  - PARS-05
  - PARS-06
  - PARS-07

must_haves:
  truths:
    - "All parsed data types are defined as frozen dataclasses with correct field types"
    - "Regex patterns compile without error and match RAPID syntax samples"
    - "Test fixtures contain representative .mod content for all move types and edge cases"
    - "pytest runs and discovers test stubs (all skip or fail -- red state for TDD)"
  artifacts:
    - path: "src/rapid_viewer/parser/tokens.py"
      provides: "RobTarget, JointTarget, MoveInstruction, MoveType, ParseResult dataclasses"
      exports: ["RobTarget", "JointTarget", "MoveInstruction", "MoveType", "ParseResult"]
    - path: "src/rapid_viewer/parser/patterns.py"
      provides: "Compiled regex patterns for RAPID syntax"
      exports: ["RE_ROBTARGET_DECL", "RE_JOINTTARGET_DECL", "RE_MOVEL", "RE_MOVEJ", "RE_MOVEC", "RE_MOVEABSJ", "RE_OFFS", "RE_WOBJ", "RE_MODULE", "RE_PROC", "RE_ENDPROC"]
    - path: "tests/test_parser.py"
      provides: "Test stubs for all parser requirements"
      contains: "test_parse_movel"
    - path: "tests/fixtures/simple.mod"
      provides: "Basic test fixture with MoveL and MoveJ"
      contains: "MoveL"
  key_links:
    - from: "tests/test_parser.py"
      to: "src/rapid_viewer/parser/tokens.py"
      via: "import MoveType, RobTarget, MoveInstruction, ParseResult"
      pattern: "from rapid_viewer\\.parser\\.tokens import"
    - from: "tests/conftest.py"
      to: "tests/fixtures/"
      via: "pathlib Path to fixture files"
      pattern: "fixtures"
---

<objective>
Establish the project skeleton, data model contracts, regex patterns, and test infrastructure for Phase 1.

Purpose: Define all type contracts and test scaffolds BEFORE implementation begins, enabling TDD workflow in Plan 02. This is Wave 0 (test infrastructure) combined with the interface-first contract definitions.

Output: pyproject.toml, parser dataclasses (tokens.py), compiled regex patterns (patterns.py), test fixture .mod files, and test stubs that import the contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-and-file-loading/01-RESEARCH.md
@.planning/phases/01-parser-and-file-loading/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton and data model contracts</name>
  <files>
    pyproject.toml,
    src/rapid_viewer/__init__.py,
    src/rapid_viewer/parser/__init__.py,
    src/rapid_viewer/parser/tokens.py,
    src/rapid_viewer/parser/patterns.py,
    src/rapid_viewer/ui/__init__.py
  </files>
  <read_first>
    .planning/phases/01-parser-and-file-loading/01-RESEARCH.md
  </read_first>
  <action>
1. Create `pyproject.toml` with:
   - `[project]` name="rapid-viewer", version="0.1.0", requires-python=">=3.11"
   - `dependencies = ["PyQt6>=6.10", "numpy>=1.26"]`
   - `[project.optional-dependencies]` dev = ["pytest>=9.0", "pytest-qt>=4.4"]
   - `[tool.pytest.ini_options]` testpaths = ["tests"], pythonpath = ["src"]
   - `[tool.ruff]` line-length = 100

2. Create `src/rapid_viewer/__init__.py` with `__version__ = "0.1.0"`

3. Create `src/rapid_viewer/parser/__init__.py` that re-exports:
   `from rapid_viewer.parser.tokens import RobTarget, JointTarget, MoveInstruction, MoveType, ParseResult`

4. Create `src/rapid_viewer/parser/tokens.py` with frozen dataclasses exactly as specified in RESEARCH.md Pattern 3:
   - `class MoveType(Enum)` with MOVEL, MOVEJ, MOVEC, MOVEABSJ
   - `@dataclass(frozen=True) class RobTarget` with fields: name (str), pos (np.ndarray), orient (np.ndarray), confdata (tuple[int, ...]), extjoint (tuple[float, ...]), source_line (int)
   - `@dataclass(frozen=True) class JointTarget` with fields: name (str), robax (tuple[float, ...]), extax (tuple[float, ...]), source_line (int)
   - `@dataclass(frozen=True) class MoveInstruction` with fields: move_type (MoveType), target (RobTarget | None), circle_point (RobTarget | None), joint_target (JointTarget | None), speed (str), zone (str), tool (str), wobj (str), source_line (int), has_cartesian (bool) with default True
   - `@dataclass class ParseResult` (NOT frozen) with fields: module_name (str), moves (list[MoveInstruction]), targets (dict[str, RobTarget]), joint_targets (dict[str, JointTarget]), source_text (str), procedures (list[str]) with default_factory=list, errors (list[str]) with default_factory=list
   - Add `__eq__` override on RobTarget and JointTarget that uses `np.array_equal` for ndarray fields (frozen dataclass default `__eq__` will fail on ndarray comparison)

5. Create `src/rapid_viewer/parser/patterns.py` with ALL compiled regex patterns from RESEARCH.md "Regex Patterns" section:
   - `_NUM = r'[-+]?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?'`
   - `_TARGET_REF = r'(\w+|Offs\s*\([^)]+\)|\[.+?\])'`
   - `RE_ROBTARGET_DECL` matching `[LOCAL] CONST|PERS|VAR robtarget name := [data]`
   - `RE_JOINTTARGET_DECL` matching `[LOCAL] CONST|PERS|VAR jointtarget name := [data]`
   - `RE_MOVEL`, `RE_MOVEJ`, `RE_MOVEC`, `RE_MOVEABSJ` for move instructions
   - `RE_OFFS` for Offs(name, dx, dy, dz)
   - `RE_WOBJ` for \WObj:=name
   - `RE_MODULE`, `RE_PROC`, `RE_ENDPROC`, `RE_ENDMODULE` for module/proc boundaries
   - `RE_BRACKET_GROUP = re.compile(r'\[([^\[\]]+)\]')` for nested bracket extraction
   - All patterns use `re.IGNORECASE`

6. Create `src/rapid_viewer/ui/__init__.py` (empty, placeholder for Plan 03)
  </action>
  <verify>
    <automated>cd "C:/Users/sovia/OneDrive/Documents/00_Work Space/Program/01_Toolpath_Simulator" && python -c "from rapid_viewer.parser.tokens import RobTarget, JointTarget, MoveInstruction, MoveType, ParseResult; from rapid_viewer.parser.patterns import RE_ROBTARGET_DECL, RE_MOVEL, RE_MOVEC, RE_MOVEABSJ, RE_OFFS; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class MoveType" src/rapid_viewer/parser/tokens.py` returns 1
    - `grep -c "@dataclass" src/rapid_viewer/parser/tokens.py` returns at least 4 (RobTarget, JointTarget, MoveInstruction, ParseResult)
    - `grep -c "frozen=True" src/rapid_viewer/parser/tokens.py` returns 3
    - `grep "has_cartesian" src/rapid_viewer/parser/tokens.py` contains "bool" and "True"
    - `grep -c "re.compile" src/rapid_viewer/parser/patterns.py` returns at least 10
    - `grep "RE_ROBTARGET_DECL" src/rapid_viewer/parser/patterns.py` exists
    - `grep "RE_MOVEC" src/rapid_viewer/parser/patterns.py` exists with two _TARGET_REF captures
    - `grep "9E" src/rapid_viewer/parser/patterns.py` or `grep "eE" src/rapid_viewer/parser/patterns.py` confirms scientific notation support in _NUM
    - `grep "testpaths" pyproject.toml` shows "tests"
    - `grep "pythonpath" pyproject.toml` shows "src"
    - `python -c "from rapid_viewer.parser.tokens import MoveType; print(MoveType.MOVEL)"` succeeds
  </acceptance_criteria>
  <done>
    All dataclass types importable from rapid_viewer.parser. All regex patterns compile. pyproject.toml configures pytest with src/ on pythonpath. Package structure src/rapid_viewer/parser/ and src/rapid_viewer/ui/ exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures and test stubs</name>
  <files>
    tests/__init__.py,
    tests/conftest.py,
    tests/fixtures/simple.mod,
    tests/fixtures/multiline.mod,
    tests/fixtures/movecircular.mod,
    tests/fixtures/moveabsj.mod,
    tests/fixtures/offs_inline.mod,
    tests/test_parser.py
  </files>
  <read_first>
    .planning/phases/01-parser-and-file-loading/01-RESEARCH.md,
    src/rapid_viewer/parser/tokens.py
  </read_first>
  <action>
1. Create `tests/__init__.py` (empty)

2. Create `tests/conftest.py` with:
   - `import pytest` and `from pathlib import Path`
   - `FIXTURES_DIR = Path(__file__).parent / "fixtures"`
   - `@pytest.fixture` for `fixtures_dir` returning FIXTURES_DIR
   - `@pytest.fixture` for `simple_mod` returning `(FIXTURES_DIR / "simple.mod").read_text()`
   - `@pytest.fixture` for `multiline_mod` returning `(FIXTURES_DIR / "multiline.mod").read_text()`
   - `@pytest.fixture` for `movec_mod` returning `(FIXTURES_DIR / "movecircular.mod").read_text()`
   - `@pytest.fixture` for `moveabsj_mod` returning `(FIXTURES_DIR / "moveabsj.mod").read_text()`
   - `@pytest.fixture` for `offs_mod` returning `(FIXTURES_DIR / "offs_inline.mod").read_text()`

3. Create test fixture files with VALID RAPID syntax:

   `tests/fixtures/simple.mod`:
   ```
   MODULE SimpleTest
     CONST robtarget p10 := [[500,0,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];
     CONST robtarget p20 := [[600,100,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];
     CONST robtarget p30 := [[700,200,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];

     PROC main()
       MoveJ p10, v1000, z50, tool0;
       MoveL p20, v100, fine, tool0;
       MoveL p30, v200, z10, tool0 \WObj:=wobj0;
     ENDPROC
   ENDMODULE
   ```

   `tests/fixtures/multiline.mod`:
   ```
   MODULE MultilineTest
     CONST robtarget pStart :=
       [[100.5, 200.3, 300.1],
        [0.707107, 0, 0.707107, 0],
        [0, 0, 0, 0],
        [9E+09, 9E+09, 9E+09, 9E+09, 9E+09, 9E+09]];
     CONST robtarget pEnd :=
       [[400, 500, 600],
        [1, 0, 0, 0],
        [0, 0, 0, 0],
        [9E+09, 9E+09, 9E+09, 9E+09, 9E+09, 9E+09]];

     PROC main()
       MoveL pStart, v100, fine, tool0;
       MoveL pEnd, v100, fine, tool0;
     ENDPROC
   ENDMODULE
   ```

   `tests/fixtures/movecircular.mod`:
   ```
   MODULE MoveCircTest
     CONST robtarget pCirStart := [[500,0,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];
     CONST robtarget pCirPoint := [[550,50,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];
     CONST robtarget pCirEnd := [[600,0,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];

     PROC main()
       MoveJ pCirStart, v1000, z50, tool0;
       MoveC pCirPoint, pCirEnd, v200, z10, tool0;
     ENDPROC
   ENDMODULE
   ```

   `tests/fixtures/moveabsj.mod`:
   ```
   MODULE MoveAbsJTest
     CONST jointtarget jHome := [[0,0,0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];
     CONST robtarget p10 := [[500,0,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];

     PROC main()
       MoveAbsJ jHome, v1000, z50, tool0;
       MoveL p10, v100, fine, tool0;
     ENDPROC
   ENDMODULE
   ```

   `tests/fixtures/offs_inline.mod`:
   ```
   MODULE OffsTest
     CONST robtarget pBase := [[500,0,400],[1,0,0,0],[0,0,0,0],[9E+09,9E+09,9E+09,9E+09,9E+09,9E+09]];

     PROC main()
       MoveL pBase, v100, fine, tool0;
       MoveL Offs(pBase, 0, 100, 0), v100, fine, tool0;
       MoveL Offs(pBase, 0, 200, 50), v100, z10, tool0;
     ENDPROC
   ENDMODULE
   ```

4. Create `tests/test_parser.py` with test STUBS that import from the parser module and use `pytest.skip("Parser not implemented yet")` as the body. Include the following test functions:
   - `test_parse_movel(simple_mod)` -- PARS-01: asserts MoveL instructions are found with correct target references
   - `test_parse_movej(simple_mod)` -- PARS-02: asserts MoveJ instruction found
   - `test_parse_movec(movec_mod)` -- PARS-03: asserts MoveC found with both circle_point and target populated
   - `test_parse_moveabsj(moveabsj_mod)` -- PARS-04: asserts MoveAbsJ found with has_cartesian=False and joint_target populated
   - `test_parse_robtarget(simple_mod)` -- PARS-05: asserts robtarget pos/orient extracted with correct values
   - `test_multiline_robtarget(multiline_mod)` -- PARS-06: asserts multiline robtarget parsed with correct position values
   - `test_line_numbers(simple_mod)` -- PARS-07: asserts each MoveInstruction has correct source_line
   - `test_offs_resolution(offs_mod)` -- Offs() target resolved with offset applied to base position
   - `test_wobj_capture(simple_mod)` -- asserts wobj field captured when \WObj parameter present
   - `test_module_name_extracted(simple_mod)` -- asserts ParseResult.module_name == "SimpleTest"
   - `test_procedures_found(simple_mod)` -- asserts "main" in ParseResult.procedures

   Each stub should have a one-line docstring stating the requirement ID it covers (e.g., `"""PARS-01: MoveL parsing"""`)
  </action>
  <verify>
    <automated>cd "C:/Users/sovia/OneDrive/Documents/00_Work Space/Program/01_Toolpath_Simulator" && python -m pytest tests/test_parser.py -v --co 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/simple.mod tests/fixtures/multiline.mod tests/fixtures/movecircular.mod tests/fixtures/moveabsj.mod tests/fixtures/offs_inline.mod` all exist
    - `grep -c "def test_" tests/test_parser.py` returns at least 11
    - `grep "test_parse_movel" tests/test_parser.py` exists
    - `grep "test_parse_movec" tests/test_parser.py` exists
    - `grep "test_parse_moveabsj" tests/test_parser.py` exists
    - `grep "test_multiline_robtarget" tests/test_parser.py` exists
    - `grep "test_line_numbers" tests/test_parser.py` exists
    - `grep "test_offs_resolution" tests/test_parser.py` exists
    - `grep "PARS-01" tests/test_parser.py` exists
    - `grep "PARS-06" tests/test_parser.py` exists
    - `grep "MoveL" tests/fixtures/simple.mod` exists
    - `grep "MoveC" tests/fixtures/movecircular.mod` exists
    - `grep "MoveAbsJ" tests/fixtures/moveabsj.mod` exists
    - `grep "Offs" tests/fixtures/offs_inline.mod` exists
    - `grep "9E+09" tests/fixtures/simple.mod` confirms scientific notation in fixtures
    - `python -m pytest tests/test_parser.py --co` lists all test names without import errors
  </acceptance_criteria>
  <done>
    All 5 fixture .mod files exist with valid RAPID syntax. test_parser.py has 11+ test stubs covering PARS-01 through PARS-07 plus Offs, wobj, module_name, and procedures. pytest --co discovers all tests without import errors.
  </done>
</task>

</tasks>

<verification>
- `python -c "from rapid_viewer.parser.tokens import MoveType, RobTarget, MoveInstruction, ParseResult; print('tokens OK')"` succeeds
- `python -c "from rapid_viewer.parser.patterns import RE_ROBTARGET_DECL, RE_MOVEL, RE_MOVEC; print('patterns OK')"` succeeds
- `python -m pytest tests/test_parser.py --co` discovers 11+ tests
- All fixture files are valid RAPID syntax (manual spot check)
</verification>

<success_criteria>
- Project skeleton created with pyproject.toml, src/rapid_viewer/ package structure
- All 5 dataclasses (MoveType enum + RobTarget + JointTarget + MoveInstruction + ParseResult) importable
- All 12+ regex patterns compile and are importable from patterns.py
- 5 fixture .mod files cover: simple moves, multiline robtarget, MoveC, MoveAbsJ, Offs()
- 11+ test stubs in test_parser.py are discovered by pytest
- No implementation of parse_module yet -- contracts only
</success_criteria>

<output>
After completion, create `.planning/phases/01-parser-and-file-loading/01-01-SUMMARY.md`
</output>
