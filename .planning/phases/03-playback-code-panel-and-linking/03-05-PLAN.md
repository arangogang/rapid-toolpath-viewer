---
phase: 03-playback-code-panel-and-linking
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - PLAY-04
  - REND-04

must_haves:
  truths:
    - "Current waypoint is visually distinct (larger, different color) in the 3D view"
    - "TCP orientation triads (RGB axis lines) are visible at each waypoint"
    - "Step forward/backward updates both 3D highlight and code panel highlight"
    - "Clicking a waypoint in 3D scrolls code panel to the correct line"
    - "Clicking a Move line in code panel highlights the correct waypoint in 3D"
    - "PROC selector filters displayed path to selected procedure only"
    - "Play button auto-advances through waypoints with speed control"
  artifacts:
    - path: "src/rapid_viewer/ui/main_window.py"
      provides: "Complete Phase 3 integration -- confirmed by human visual inspection"
    - path: "src/rapid_viewer/renderer/toolpath_gl_widget.py"
      provides: "Highlight marker and TCP triads -- confirmed by human visual inspection"
  key_links:
    - from: "Human eyes"
      to: "Application UI"
      via: "Visual and interaction verification of all Phase 3 features"
      pattern: "N/A -- manual verification"
---

<objective>
Visual verification checkpoint for Phase 3: confirm all interactive features work correctly in the running application.

Purpose: PLAY-04 (visual highlight) and REND-04 (TCP triads) can only be verified by human visual inspection. This checkpoint also validates the full end-to-end workflow that automated tests cannot fully exercise (real GL rendering, mouse interaction, visual feedback).

Output: Go/no-go decision. Issues found here feed back into gap closure plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-playback-code-panel-and-linking/03-VALIDATION.md
@.planning/phases/03-playback-code-panel-and-linking/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and launch application</name>
  <files></files>
  <read_first>
    - tests/fixtures/ (check what .mod fixture files exist)
    - .planning/phases/03-playback-code-panel-and-linking/03-VALIDATION.md (validation checklist)
  </read_first>
  <action>
Run the full test suite one final time to confirm all automated tests are green:

```
python -m pytest tests/ -v --tb=short
```

Then launch the application:

```
python -m rapid_viewer.main
```

Load the multiproc test fixture (tests/fixtures/multiproc.mod) via File > Open, or any available .mod file with multiple PROCs and mixed move types. Note any crash or error.
  </action>
  <verify>
    <automated>cd "C:/Users/sovia/OneDrive/Documents/00_Work Space/Program/01_Toolpath_Simulator" && python -m pytest tests/ -q --tb=short</automated>
  </verify>
  <done>
    - All automated tests pass (no red)
    - Application launches without crash
    - A .mod file loads without error dialog
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3: playback controls, RAPID code panel with syntax highlighting, bidirectional 3D-to-code linking, PROC selector, TCP orientation triads, and current waypoint highlight.
  </what-built>
  <how-to-verify>
Load a .mod file (e.g., tests/fixtures/multiproc.mod) and verify each feature:

**Layout:**
1. Confirm 3D view on left, code panel on right, playback toolbar at bottom
2. Confirm splitter handle between 3D view and code panel is draggable

**Syntax highlighting (CODE-02):**
3. MoveL/MoveJ keywords are teal and bold
4. PROC/ENDPROC are purple and bold
5. CONST/robtarget are blue
6. Comments (lines with !) are green

**Step playback (PLAY-01, PLAY-02, PLAY-05):**
7. Click >> (step forward) -- position label updates "1 / N" -> "2 / N"
8. Click << (step backward) -- position label decrements
9. Current waypoint is visually distinct in 3D view (PLAY-04): larger point, different color

**Code line highlight (CODE-03):**
10. As you step through waypoints, code panel scrolls to and highlights the corresponding Move line

**Auto-play (PLAY-03, PLAY-06):**
11. Click Play -- waypoints advance automatically
12. Drag speed slider -- confirm speed changes

**Scrubber (PLAY-07):**
13. Drag scrubber slider -- confirm instant jump to any position

**3D click to code (LINK-01):**
14. Click a waypoint marker in the 3D view -- code panel scrolls to corresponding line

**Code click to 3D (LINK-02):**
15. Click a MoveL/MoveJ line in code panel -- corresponding waypoint highlights in 3D

**PROC selector (PARS-08):**
16. Select a specific PROC from combo box -- only that PROC's path displayed
17. Select "All PROCs" to restore full view

**TCP triads (REND-04):**
18. Small RGB axis lines visible at each waypoint (R=X, G=Y, B=Z)
  </how-to-verify>
  <resume-signal>
Type "approved" if all 18 checks pass.
Or describe any failing checks and this phase will create a gap closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
- All automated tests green: `python -m pytest tests/ -v`
- Human approves all 18 visual/interaction checks above
</verification>

<success_criteria>
- Automated test suite: 100% green
- Visual: current waypoint highlighted with distinct marker
- Visual: TCP triads visible at each waypoint
- Visual: syntax highlighting with correct color categories
- Interaction: step forward/backward with position indicator
- Interaction: auto-play with speed control and scrubber
- Interaction: bidirectional 3D-to-code and code-to-3D navigation
- Interaction: PROC selector filters path correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-playback-code-panel-and-linking/03-05-SUMMARY.md`
</output>
